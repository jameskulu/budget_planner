import { View } from 'react-native';

/**
 * Placeholder screen for the center tab slot. The tab bar renders a raised
 * RecordFab here instead of a navigable tab, so this screen is never shown.
 */
export default function RecordSlotScreen() {
  return <View style={{ flex: 1 }} />;
}
